#ifndef FILE_UTILITIES
#define FILE_UTILITIES

#include <fstream>
#include <string>

namespace acc {
namespace utils {

std::string read_file_contents(std::string &file_name);

} // namespace utils
} // namespace acc

#endif
